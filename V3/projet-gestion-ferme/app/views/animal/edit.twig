<!doctype html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, user-scalable=no, initial-scale=1.0, maximum-scale=1.0, minimum-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <title>Modifier Produit</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
<div class="container mt-4">
    <div class="row">
        <div class="col-6">
            <h4 class="text-center">Modifier Animal</h4>
            <form action="?controller=animal&action=updateEedit" method="POST" class="container">
                <input type="hidden" name="id" value="{{ animal.getId() }}">

                <div class="mb-3">
                    <label for="libelle" class="form-label">Type</label>
                    <input type="text" autofocus name="type" value="{{ animal.getType() }}" class="form-control" required>
                </div>

                <div class="mb-3">
                    <label for="pu" class="form-label">Age</label>
                    <input type="number" min="0" name="age" value="{{ animal.getAge() }}" class="form-control" required>
                </div>

                <div class="mb-3">
                    <label for="qt" class="form-label">Santé</label>
                    <input type="text" name="sante" value="{{ animal.getSante() }}" class="form-control" required>
                </div>

                <div class="mb-3">
                    <label for="equipment_id" class="form-label">Equipment</label>
                    <select id="equipment_id" name="equipment_id" class="form-control" required>
                        <option value="">Sélectionnez un équipment</option>
                        {% for e in equipments %}
                            <option value="{{ e.id }}"
                                    {{ e.id == animal.equipment.id ? 'selected' : '' }}>
                                {{ e.nom }}
                            </option>
                        {% endfor %}
                    </select>
                </div>
                <div class="text-center">
                    <button type="submit" class="btn btn-success">Modifier</button>
                </div>
            </form>
        </div>
    </div>
</div>
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
